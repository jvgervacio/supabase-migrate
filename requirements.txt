# Python dependencies for the storage half (Amazon S3 -> Supabase Storage).
#
#     pip3 install -r requirements.txt
#
# The database half does not use Python at all -- it shells out to pgloader,
# which is a system package, not a pip install:
#
#     sudo apt-get install -y pgloader
#
# botocore is pulled in automatically as a boto3 dependency. Versions from
# 1.36 onward attach a CRC32 checksum header that Supabase Storage rejects;
# migrate.py disables it explicitly, so both older and newer releases work.

boto3>=1.34.0
